Use `ScoreRing` for a single percentage beside a title.

```jsx
<ScoreRing percent={30} />
<ScoreRing percent={20} size={84} />
```

Results use a big numeral plus a sage `ProgressBar` instead of a ring.
