Use `StreakStrip` at the top of the board home. It is its own panel — don't wrap it in a `Card`.

```jsx
<StreakStrip days={12} todayDone={2} todayTotal={5} />
```

Sun means momentum only; never use it to signal a correct answer.
