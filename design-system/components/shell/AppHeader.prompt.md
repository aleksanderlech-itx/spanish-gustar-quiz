Use `AppHeader` at the top of any non-quiz screen.

```jsx
<AppHeader title="Quiz Studio" badge={12} />
<AppHeader title="Ser vs estar" onBack={goBack} />
```

Stays on the paper-lighter surface with a 2px ink underline; never elevated.
